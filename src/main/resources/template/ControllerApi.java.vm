package ${package}.api.${moduleName};

import com.tentwelve.model.response.QueryResponseResult;
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import ${package}.entity.${moduleName}.${className};



/**
 * ${comments}
 *
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */

@Api(value="${comments}操作接口",description = "提供${comments}CRUD")
public interface ${className}ControllerApi {

    @ApiOperation("分页查询")
    public QueryResponseResult queryPage(Integer size, Integer current, ${className} ${classname}) ;


}
